$(document).ready(function() {

    // Variable de la page inscription
    let input = $('.input_password');
    const btn = $('#show_password');

    // Lancement de la fonction avec les paramètres adéquat
    showPassword(btn, input);
});