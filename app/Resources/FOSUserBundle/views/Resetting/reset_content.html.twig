<div class="row mt-60">
    <div class="col s12 m8 offset-m2 l6 offset-l3 xl6 offset-xl3">
        {{ form_start(form, { 'action': path('fos_user_resetting_reset', {'token': token}), 'attr': { 'class': 'fos_user_resetting_reset' } }) }}
            <div class="card">
                <div class="card-content white-text">
                    <span class="card-title center-align">{{ 'resetting.reset.submit'|trans({}, 'FOSUserBundle') }}</span>
                    <div class="input-field mt-40">
                        {{ form_widget(form.plainPassword.first) }}
                        {{ form_label(form.plainPassword.first) }}
                    </div>
                    <div class="input-field">
                        {{ form_widget(form.plainPassword.second) }}
                        {{ form_label(form.plainPassword.second) }}
                    </div>
                </div>
                <div class="card-action">
                    <div class="row">
                        <div class="col s12 m12 l12 xl12">
                            <button class="btn btn-reset-pwd btn-large hoverable deep-orange" type="submit">
                                <i class="material-icons left">check</i>
                                {{ 'resetting.reset.button'|trans({}, 'FOSUserBundle') }}
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        {{ form_end(form) }}
    </div>
</div>

